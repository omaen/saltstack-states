#
# This file is managed by Salt. Changes will be overwritten!
#
{% for proxy_url in config.proxies %}
Acquire::http::Proxy "{{ proxy_url }}";
{%- endfor %}
{%- for direct_url in config.direct %}
Acquire::http::proxy::{{ direct_url }} "DIRECT";
{%- endfor %}
