{{ salt['pillar.get']('salt:managed_file_header', '# This file is managed by Salt') }}

Defaults env_keep = "http_proxy https_proxy no_proxy"

{%- for name, sudoers in salt['pillar.get']('sudoers', {}).items() %}
{%- for sudoer in sudoers %}
{{ sudoer }}
{%- endfor %}
{%- endfor %}
