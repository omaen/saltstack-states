{{ salt['pillar.get']('salt:managed_file_header', '# This file is managed by Salt') }}
{%- for module, params in salt['pillar.get']('rsync:modules').iteritems() %}

[{{ module }}]
{%- for key, value in params.iteritems() %}
    {{ key }} = {{ value }}
{%- endfor %}
{%- endfor %}
