squid:
  package: squid
  service: squid
  squid_conf: /etc/squid/squid.conf
  config: {}
  acl_dir: /etc/squid/acls
  blacklist_update_script: /usr/local/bin/update-squid-blacklists
  blacklists: {}
  cert_dir: /etc/squid/ssl
  certificates: {}
  keytab:
    name: /etc/squid/squid.keytab
