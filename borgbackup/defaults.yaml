borgbackup:
  borgbackup: borgbackup
  config:
    client:
      ssh_key_name: /root/.ssh/id_rsa
      auto_add_servers: []
      force_server_update: False
    server:
      user: borg
      group: borg
      backup_dir: /srv/borg
      auto_add_allowed: True

Debian: {}
